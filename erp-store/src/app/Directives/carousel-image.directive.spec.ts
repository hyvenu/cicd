import { CarouselImageDirective } from './carousel-image.directive';

describe('CarouselImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselImageDirective();
    expect(directive).toBeTruthy();
  });
});
